<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-800 py-1">
      <a class="mt-10 mx-9 text-blue-700" href="/">Go to home</a>
      <h1 class="text-3xl tracking-tighter mt-10 text-white mx-8">All users</h1>
      <hr class="text-zinc-900 my-5">
      <div class="users gap-3 flex flex-wrap ">
      <% if(users.length > 0) { %> 
          <% users.forEach(function(user){ %>
           <div class="user mx-7 my-7 bg-zinc-300 max-h-90 w-72 px-2 py-2 " >
           <div class="h-40 w-full bg-zinc-400 rounded-lg overflow-hidden"><img class="w-full h-full object-cover object-center " src="<%=user.image%>" alt=""></div>
           <h3 class="text-3xl "><%=user.name %> </h1>
            <h5 class="px-3"><%=user.email %></h3>
            <div class="flex gap-3 px-3">
            <a class="text-blue-800" href="/edit/<%=user._id %>">Edit</a>
            <a class="text-red-600 ml-5" href="/delete/<%= user._id %>">Delete</a>
            </div>
        </div>
      </div> 
        <% }) %>
      <%}else{ %> <h2 class="text-3xl text-white mx-10 mt-8 ">No Read File</h2><% }
      %>
   </div>   
</body>
</html>
    
