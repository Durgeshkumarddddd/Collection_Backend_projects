<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
<div class="w-full min-h-screen bg-zinc-800 py-1">
    <div class="form  mt-10 mx-5 px-5 py-1 ">
        <form action="/createpost" method="post">
            <input class="block w-3/5 rounded-md mt-3 px-3 py-3 " type="text" name="title" placeholder="Title">
         <textarea class="block w-3/5 rounded-md mt-3 px-3 py-3 "  name="details" placeholder="Description"></textarea>
            <input class="block font-semibold rounded-md mt-3 px-3 py-3 bg-red-700 text-white" type="submit" name="Create Post"placeholder="submit" >  
         </form>
  </div>
  
  <div class="  mx-10 mt-7 flex-wrap ">
  <% if(files.length > 0){ %>
    <% files.forEach(function(value){ %>

        <div class="px-5 py-4 inline-block rounded-md bg-zinc-500 w-1/4 min-h-20 mx-2">
              <h1 class="text-white text-3xl tracking-tighter"><%= value %></h1>
              <a href="" class="text-blue-900 inline-block mt-3 font-semibold"> <%=value %> </a>
        </div>
  
   <% }) %>
   <% } else{ %>   
      <h3 class="px-5 py-4 mt-7 mx-10 block rounded-md bg-zinc-500 w-1/4 min-h-20 text-white"> No text file uploaded </h3>
    <%} %> 
  </div>
</div>
</body>
</html>
